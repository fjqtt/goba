# Generator — checklist реализации

Источник требований: [PLAN.md](PLAN.md), roadmap §16.

- [x] G0.0 Создать отдельную папку проекта, сохранить план и связь с Client.
- [ ] G0.1 Pin repos, license inventory, build feasibility rzone, mapping, 20 known fixtures. **Частично:** зависимости закреплены; rzone source/image digests, GPL и build blocker зафиксированы в `RZS_FEASIBILITY.md`. OrbStack запускает amd64 под эмуляцией, но не предоставляет NVIDIA/CUDA runtime. Альтернативы проверены в `MAC_SOLVER_EVALUATION.md`: GNU Go выбран как Mac CPU candidate lane; KataGo Metal — ranking lane. На 37 Cho fixtures GNU Go совпал первым ходом в 27 и принял авторский ход в 33. Нужны 20 принятых cross-engine fixtures и Linux/amd64 CUDA worker для RZS proof lane.
- [ ] G1.1 SGF import, draft metadata, editor, common schema, replay. **Частично:** SGF collection импортируется в детерминированные drafts, setup/PL/viewport/provenance/target candidates сохраняются, цель подтверждается с optimistic revision. Restricted printable key разбирается отдельно и получает `linkedDraftId` только при exact setup match. 55 несовпадений разобраны: 46 — flattened numbered overlay, 1 malformed row восстановлен, 8 — настоящие различия постановок. Нет editor, persistence и нормализации proof tree в ProblemV1.
- [ ] G2.1 CPU/RZS adapters, job leases, artifacts, gold corpus. **Частично:** `tsumego.js` worker имеет hard timeout/memory/output limits. Реализован GNU Go Owl adapter с wall/output/tree limits, binary/config/input hashes, GTP-проверкой альтернатив и raw SGF tree; API dispatch принимает `gnugo-owl`. Его вывод намеренно остаётся `unknown/heuristic-candidate-only`. Очередь пока in-memory без lease; RZS и независимый replay отсутствуют.
- [ ] G3.1 Branch expansion, alternatives, wrong branches/refutations, measured coverage. **Частично:** все 845 exact-linked Cho positions прошли GNU Go batch audit. Сохранены 3 327 candidate trees; вся доступная source line найдена в 374/723, из них только 305 имеют полностью легальное выбранное дерево. Все ветви replay-ятся; дополнительные/отсутствующие ходы остаются `unclassified`. KataGo Metal проверил 2 642 состояния перед каждым доступным ходом ключа по AGA: 0 API errors; из 174 прямых расхождений GNU Go ↔ key policy KataGo ставит key выше в 126, GNU Go-вариант — в 48. Нет terminal verifier, нормализованного ProblemV1 и refutation expansion.
- [ ] G4.1 OpenCV ingestion, correction UI, provenance.
- [ ] G5.1 Immutable publishing/revocation, costs, 100–300 curated tasks.

## Последний checkpoint — 2026-09-16 18:13 Europe/Lisbon

- Client припаркован; активный проект — Generator.
- Полный `/private/tmp/cho-1.sgf` импортирован: **900 drafts**, hash `sha256:2667ddc1f73d9256820a598427ae212be78874c33932c5baf99d97793d8df432`, готовых деревьев 0, все позиции B to play.
- Первая позиция Чо проверена по всем 4 найденным группам-кандидатам. `tsumego.js` честно вернул `unsupported`: связанная пустая область содержит 347–350 пунктов при лимите lane ≤15. Значит, полный том требует RZS/general-local solver либо явно подтверждённой локальной boundary model; синтетическую стенку добавлять нельзя.
- Эталон `tsumego.js` проходит: для documented fixture найдены `W[bf]` и `W[bg]`, строится partial tree.
- Rzone закреплён на source commit `325e0ecd...`; официальный image `sha256:1d1b6bab...` имеет только `linux/amd64`, 2.21 GiB compressed и Caffe2/CUDA runtime. На текущем arm64 host feasibility blocked; образ не загружался.
- OrbStack проверен отдельно: `linux/amd64` контейнеры работают через эмуляцию (`x86_64`), но NVIDIA runtime и GPU devices отсутствуют. Сборка rzone без Caffe2 компилируемо выглядит только формально: network backend остаётся неинициализированным, поэтому полноценный CPU port потребует изменений inference-кода.
- Добавлен `match-corpus`: из 50 upstream `chao_vol1` records лишь 3 точно совпали с текущими 900 позициями даже с учётом симметрий.
- Нативный GNU Go 3.8 установлен через Homebrew и подключён отдельным adapter. Реальный probe задачи Cho №8 дал root `A18`, сохранил SGF с 10 attack / 8 defense variations и также предложил непроверенную альтернативу `H19`.
- Добавлена команда `probe:gnugo`: выбирает диапазон задач скачанного SGF, перебирает target hypotheses, печатает JSONL и сохраняет деревья в отдельный каталог.
- Точность GNU Go измерена на 37 upstream Cho fixtures: 27/37 совпадений primary move, 33/37 авторских moves приняты как работающие; это candidate lane, а не proof.
- Добавлен restricted printable-key parser и `link:printable-key`. Из 900 записей community key строго связаны 845; 55 setup отличаются, problem 201 содержит malformed row. Manifest: `/private/tmp/goba-cho-answer-links.json`; перенос по одному номеру запрещён.
- Полный `audit:gnugo-book` обработал 845/845 связанных задач за 83.35 s. Source line полностью присутствует в 374/723; 69 из этих деревьев содержат другие незаконные ветви, строгий clean set — 305. Частичное продолжение — 73, только root/не все root alternatives — 392, clean all-root coverage — 1, no matching target — 5.
- Сохранены 3 327 raw SGF trees (123 MiB). Ни один export не упал. 189 задач имеют illegal branch хотя бы в одном candidate tree; причины: `wrong-turn` и `superko`. 459 задач имеют дополнительные root candidates, которые остаются `unclassified`.
- Отчёт: `/private/tmp/goba-cho-gnugo-audit/audit-report.json`, SHA-256 `c283aed19995101909d3122e293be00dab308e8aff00931735398aad3439ac2d`. Читаемая сводка: `CHO_BOOK_AUDIT.md`.
- 55 setup mismatches разобраны детерминированным `reconcile:printable-key`: 46 записей совпадают с SGF, если numbered solution overlay считать отображёнными камнями; задача 201 однозначно восстановлена вставкой пустой точки после левого края; настоящие version/setup differences остались у 4, 8, 40, 93, 216, 420, 533, 617. Отчёт: `/private/tmp/goba-cho-reconciliation/reconciliation-report.json`.
- 47 восстановленных base-position candidates отдельно прогнаны GNU Go: 1 full-line, 1 partial-line, 19 root-only, 16 no-target, 10 source-line-illegal. Это reconciliation candidates, а не автоматически принятые решения.
- Добавлен воспроизводимый KataGo cross-check: `prepare-katago-crosscheck.ts`, `run-katago-analysis.ts`, `analyze-katago-crosscheck.ts`. KataGo 1.16.2 Metal завершил 2 642/2 642 состояний по AGA, 32 visits, 0 ошибок за 603 s. Проверены все доступные ходы, а не только root: 2 393 exact-linked, 197 reconciled и по 26 для key/SGF версий восьми настоящих различий.
- На exact-linked линиях key move входит в local policy top-5 у 2 366/2 393 состояний. Выбранное GNU Go дерево имеет продолжение на 1 540 префиксах и содержит следующий key move на 1 366. В 174 прямых расхождениях KataGo policy предпочитает key в 126 и GNU Go-вариант в 48.
- Triage восьми настоящих setup differences: SGF лучше поддержан у 40 и 420; key — у 533 и 617, где SGF делает ход занятым; у 216 обе линии незаконны; 4, 8, 93 неразличимы. Это ranking signal, не proof.
- Итог: `KATAGO_CROSSCHECK.md`; JSON/CSV: `/private/tmp/goba-cho-katago-crosscheck/analysis/`; review queue — 254 строки (133 generator diverges/key higher, 49 generator higher, 58 key outside top-5, 14 illegal source-line states).
- Проверка после reconciliation/KataGo: `npm run check` прошёл; 16 файлов / 50 тестов прошли; Client + Generator production build прошёл. Повторный `npm audit --omit=dev` не получил DNS в sandbox, а внешний запрос был отклонён auto-review; зависимости не менялись, последний успешный audit до этого блока показывал 0 production vulnerabilities. `npm ls --omit=dev` дополнительно обнаружил прежний unmet peer `preact` у `@sabaki/shudan`; Client build при этом проходит.
